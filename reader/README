For the most recent change pass it a {filename} with -string and the energy of each scmp event passing the tests in user_superCmpEvent.c will be output to {filename}.pE.otxt
(hopefully)
(there is a large but finite chance it'll seg fault or do something totally at random instead)
